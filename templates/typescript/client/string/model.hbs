{{#if obj.format }}
    {{#if (inList obj.format 'date' 'date-time' 'time' )}}
import { JsonObject, JsonProperty } from 'typescript-json-serializer';

@JsonObject()
export class {{ getTypeName obj }} {
    date: Date
}
    {{ else }}
export type {{ getTypeName obj }} = string
    {{/if}}
{{ else }}
export type {{ getTypeName obj }} = string
{{/if}}