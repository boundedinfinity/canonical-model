kinds:
  - id: canonical:calendar/notification
    description: Calendar Notification
    kind: object
    properties:
      - name: before
        kind: ref
        ref: canonical:time/duration
        required: true
      - name: method
        kind: string
        enum:
          - default
          - email
          - text
          - all
