id: canonical://www.boundedinfinity.com/canonical/calendar/notification
description: Calendar Notification
kind: object
properties:
    -   name: before
        $ref: canonical://www.boundedinfinity.com/canonical/time/duration
    -   name: method
        kind: string
        enum:
            - default
            - email
            - text
            - all
required:
    - before
