package repository_test

import (
	"testing"
)

func Test_RepositoryFile_Store(t *testing.T) {
	// input := model.Messages.FromType(
	// 	people.Person{
	// 		Name: people.Name{
	// 			Prefixes:    []people.Prefix{people.Prefixes[0]},
	// 			GivenNames:  []string{"James"},
	// 			FamilyNames: []string{"Bond"},
	// 			Suffixes:    []people.Suffix{people.Suffixes[0]},
	// 		},
	// 	},
	// )

	// repo, err := repository.NewFileMessageRepository(".", messenger.NewMarshaller())
	// assert.Nil(t, err)

	// err = repo.Store(input)
	// assert.Nil(t, err)
}

func Test_RepositoryFile_Load(t *testing.T) {
	// input := model.Messages.FromType(
	// 	people.Person{
	// 		Name: people.Name{
	// 			Prefixes:    []people.Prefix{people.Prefixes[0]},
	// 			GivenNames:  []string{"James"},
	// 			FamilyNames: []string{"Bond"},
	// 			Suffixes:    []people.Suffix{people.Suffixes[0]},
	// 		},
	// 	},
	// )

	// repo, err := repository.NewFileMessageRepository(".", messenger.NewMarshaller())
	// assert.Nil(t, err)

	// err = repo.Load(rfc3339date.DateTimes.Zero)
	// assert.Nil(t, err)
}
