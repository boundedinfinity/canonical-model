{{#each (getImports obj) }}
import { {{ getType this }} } from '{{ getImportPath this }}'
{{/each}}

export class {{ getName obj }} {
    {{#each obj.properties}}
    {{ @key }}{{#if (propOptional ../obj @key) }}?{{/if}}: {{ getType this }};
    {{/each}}
}
