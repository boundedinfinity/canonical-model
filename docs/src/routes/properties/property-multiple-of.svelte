<h4>mulitple-of</h4>

<p>A description of this field.</p>

<div>
    The integer constraint must be a multiple of the values.  Meaning 
    that if the result of the <code>value</code> modulo <code>multiple-of</code> evaluates 
    to <code>0</code> will satisfy this contraint.
</div>
